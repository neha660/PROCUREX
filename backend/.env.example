# Copy this file to .env and fill in your key to enable live Gemini calls.
# Leave GEMINI_API_KEY empty and ProcureX runs perfectly well on its
# deterministic offline mock (no external calls, no key needed) — useful
# for a hackathon demo with unreliable wifi.

GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
